# Security

Report a suspected vulnerability privately to security@sendrelays.com. Do not open a public issue containing credentials, installation capabilities, account data, or exploit details.

Relay releases are immutable. A release workflow must fail if an exact-version npm package or runtime object already exists with different bytes. Production promotion is one-way and requires a protected environment review.
