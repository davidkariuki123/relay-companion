// Generated by npm run agent-guide:generate. Edit packages/shared/src/agent-guide.ts.
module.exports = function invitationShareCopy(url) {
    // Relay is installed with its desktop app, never by an agent. The invitation
    // page is the download page, so both texts send the person there.
    return {
        shareText: [
            "Join me on Relay so we can message each other from our AI conversations.",
            "",
            "Open this invitation to download the Relay app:",
            url,
        ].join("\n"),
        agentPrompt: `Relay is installed with the Relay desktop app, not by an AI agent. Open ${url} in my usual browser so I can download it there, and do not install anything yourself.`,
    };
};
