// Generated by npm run agent-guide:generate. Edit packages/shared/src/agent-guide.ts.
module.exports = function invitationShareCopy(url) {
    const agentPrompt = [
        "Please set up Relay only if needed, using the instructions for agents at:",
        `${url}/agent`,
        "",
        "First check whether I already use Relay and preserve any existing setup. If setup is needed, ask my permission once to set up the Relay protocol, install and keep its agent skill updated, and install the Relay Companion. After I approve, make the direct HTTPS protocol usable immediately and continue the interactive tutorial while the Companion installs in parallel. Never read or send a Relay unless I ask, and show me the first message to my inviter before sending it.",
    ].join("\n");
    return {
        shareText: [
            "Join me on Relay so we can message each other from our AI conversations.",
            "",
            "Open the invitation yourself, or copy the setup request below into Claude Code or Codex:",
            "",
            "Please help me connect on Relay using this invitation:",
            url,
            "",
            "Open this invitation and read its current agent setup instructions. First check whether I already use Relay and preserve any existing setup. Ask for my approval before installing software or connecting my account. I will approve account access in my browser; ask separately before sending a message.",
        ].join("\n"),
        agentPrompt,
    };
};
